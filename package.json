{
  "name": "dab-timer",
  "version": "1.0.0",
  "description": "Timer app for dabbing sessions",
  "main": "index.html",
  "scripts": {
    "build": "echo 'Web assets are static in www folder' && npx cap sync android",
    "sync": "npx cap sync android",
    "android:build": "cd android && ./gradlew assembleDebug",
    "android:release": "cd android && ./gradlew assembleRelease"
  },
  "author": "twoskoops707",
  "license": "MIT",
  "dependencies": {
    "@capacitor/android": "^6.1.2",
    "@capacitor/core": "^6.1.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.1.2"
  }
}